# Copy to .env.local for local `npm run dev`. Optional — leave unset and the
# CAPTCHA widget on Login/Register just doesn't render (matches the backend's
# CAPTCHA_ENABLED=false default, so nothing breaks either side of the pair).
VITE_HCAPTCHA_SITE_KEY=
